import React from 'react';
import EventRow from './EventRow';

const Header = ({events}) => {
  return (<header className="header">
    <h1 className="title">BetEasy Front End Tech Challenge</h1>
  </header>);
};

export default Header;
